import random


def evaluate_npi(prompt: str) -> dict:
    # Placeholder: Replace with real model analysis later
    return {
        "structure": random.uniform(0.3, 0.9),
        "emotion": random.uniform(0.5, 1.0),
        "rhythm": random.uniform(0.4, 0.8),
        "context": random.uniform(0.5, 0.9),
        "strategy_tone": random.uniform(0.4, 0.8),
        "silence": random.uniform(0.6, 1.0),
        "total": 0.0,  # will be calculated
    }
